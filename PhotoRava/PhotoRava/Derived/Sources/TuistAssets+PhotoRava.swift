import SwiftUI

// Stub for Tuist generated assets
public enum PhotoRavaAsset {
    public enum Colors {
        public static let accentColor = Color("AccentColor")
    }
    public enum Assets {
    }
}
